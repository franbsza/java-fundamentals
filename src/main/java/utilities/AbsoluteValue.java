package utilities;

public class AbsoluteValue {

    // return absolute value, in other words even the value be negative return positive value
    public int convert(int numA, int numB){

        int result = Math.abs(numA - numB);
        return result;
    }
}
